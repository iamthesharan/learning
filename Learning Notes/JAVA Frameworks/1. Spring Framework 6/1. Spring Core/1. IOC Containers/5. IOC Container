There Are two Types of IOC Container:
1. BeanFactory
2. ApplicationContext

Spring IOC Container Leverages 4 Important Information:
1. Core - All the Core libraries and Core principles are adopted like IOC or DI
2. Context - Post that IOC Container Leverages the Context which is virtual Memory Location inside spring framework where all the configs and instantiaion details are kept.
             On Startup, IOC Container will look for Context.
3. Bean - Based on context and Configuration all the JAVA Objects are Instantiated in the form of beans.
4. SPEL - During Runtime by using Sprig Expression Language, it will try to inject the dependenies based on the configuration inside a context.

