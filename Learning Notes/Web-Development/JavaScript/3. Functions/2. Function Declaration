

start();

function start(){
    console.log("Start");
}

This Approach will work perfectly fine because the JS hoist the Function to the start for the file when it JS engine loads the script

============================================================

To Avoid this, use the better approach.

start();// This will throw an error.
const start = function(){
    console.log("Start");
}