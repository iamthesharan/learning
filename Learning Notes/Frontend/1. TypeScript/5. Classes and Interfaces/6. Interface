Syntax:

interface person{
    name: string; // You cannot assign the Value here. Interface is just a specification
    age: number;
    getPersonDetails(name:string):void;
}


Now Below We will Assign the Object to Interface. Below Is Valid.

const person={
    name:'Shashank',
    age:25,
    getPersonDetails(phrase:string){
        console.log(phrase +' '+this.name);
    }
}


person.getPersonDetails('Hi There - I am ');



You cannot add public or private in interface.

Interface can be implemeted on other class with iplements keyword


==================================================================

you can use readonly with variable in interface defination.

==================================================================

