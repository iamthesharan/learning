export class AccountService{
    accounts=[
        {
            name:"Master Account",
            status:'active'
        },
        {
            name:'Tested Account',
            status:'unknown'
        }
    ];
    addAccount(name:string,status:string){
        this.accounts.push(name:name,status:status);
    }
    updateStatus(id:number,status:string){
        this.account[id].status=status;
    }
}

Now we can Access the account service in the Constructor of the components 
which requires it with the help of dependency Injection.